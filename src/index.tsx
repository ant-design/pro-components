import BasicLayout, { BasicLayoutProps } from './BasicLayout';
import Footer from './Footer';
import Header, { HeaderViewProps as HeaderProps } from './Header';

export { Settings } from './defaultSettings';

export { MenuDataItem } from './typings';

export { BasicLayout, BasicLayoutProps, HeaderProps };

export const DefaultHeader = Header;
export const DefaultFooter = Footer;
